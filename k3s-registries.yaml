# Drop this file at /etc/rancher/k3s/registries.yaml and restart k3s:
#   sudo cp k3s-registries.yaml /etc/rancher/k3s/registries.yaml
#   sudo systemctl restart k3s

mirrors:
  "localhost:5000":
    endpoint:
      - "http://localhost:5000"
